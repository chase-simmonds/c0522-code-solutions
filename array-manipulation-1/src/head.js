/* exported head */
function head(array) {
  return array[0];
}

/* declare function head with one parameter array. In the code block, array at
index 0 is being returned, which is the first value of the array. */
