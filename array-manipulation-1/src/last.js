/* exported last */
function last(array) {
  return array[array.length - 1];
}

/* define function with one paramter

target length of the array minus 1 for last character.

return value of array. */
