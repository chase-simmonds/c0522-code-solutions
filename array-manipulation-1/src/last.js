/* exported last */
function last(array) {
  return array[array.length - 1];
}

/* delcare a function last with one parameter array. In the code block, the
length of the array object is being subtracted by one to account for the index
starting at 0, which gives us the last value of the array . This value is being
passed into the index of array. The value of array at the last index is being
returned. */
