/* exported getLastChar */
function getLastChar(string) {
  return string.charAt(string.length - 1);
}

/* declare function with one parameter string.

target the last character of the string by passing the string length -1 into the
argument for the charAt method of the string object.

Return the result of that expression.
*/
