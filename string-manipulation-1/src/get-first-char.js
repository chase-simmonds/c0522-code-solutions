/* exported getFirstChar */
function getFirstChar(string) {
  return string.charAt(0);
}

/* Declaring a function getfirstchar with one parameter string. In the code block
I'm returning the first character using a method targeting the 0 position of the
string, which will give me the first character of any string it that passes
through.  */
