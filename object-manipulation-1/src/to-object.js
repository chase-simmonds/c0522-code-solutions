/* exported toObject */
function toObject(keyValuePair) {
  var object = {};
  object[keyValuePair[0]] = keyValuePair[1];
  return object;
}

/* define function with one parameter.

create empty object to hold values.

assign object at key value pair at 0 to keyvaluepair at index 1.

return object value. */
