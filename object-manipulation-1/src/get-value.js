/* exported getValue */
function getValue(object, key) {
  return object[key];
}

/* define function getValue with two parameters, object and key. in the code block
for the function return object dave at index key, which returns the value of
the key that's passed through. */
