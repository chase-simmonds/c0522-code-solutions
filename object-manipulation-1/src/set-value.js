/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

/* define function setValue with three parameters: object, key and value. Goal is
to set the value as a property on the object. In the code block set the index key
of the object equal to value to do so. */
