/* exported firstChars */
function firstChars(length, string) {
  return string.slice(0, length);
}

// define function firstChars with two parameters length and string
//
// slice string starting at 0, and end at the length by passing that through
// as second argument.
//
// return the result
