/* exported lastChars */
function lastChars(length, string) {
  return string.slice(-length);
}

// define function lastChars with two parameters length and string
//
// slice the string, and pass the length argument with negative value which will
// extract from end of the string.
//
// return result of that expression
